# Grey Crow 协作入口

默认使用中文，先说结果、证据和未验证项。当前仓库是灰鸦的源码工作目录。

## 只保留必要的权威

- 用户最新明确要求决定目标、范围和授权；[PRODUCT.md](PRODUCT.md) 是稳定产品目标的唯一权威文档。
- [REBUILD.md](REBUILD.md) 只记录当前计划、进展、证据和待决定事项；代码与真实运行说明当前实际行为。
- 默认读 PRODUCT.md 与 REBUILD.md 的“当前位置”、本次涉及的代码模块与待办层；证据只在核对有关结论时打开，源码只沿本次问题查看，不要求全仓或逐份历史阅读。
- 目录 README 只做源码导航；打包、语音资源、素材和许可证资料只在对应任务按需查阅，不另设产品目标或开发状态。旧历史、任务卡和项目外资料不能恢复已取消的要求，也不能自动启动开发。
- 本仓库不使用 PROJECT_STATUS.md、project-state.json、current-gate.md 或旧控制检查器。缺少它们不构成阻塞，也不为满足旧 Skills 而重建。

## 自主推进

- 在用户确认的产品范围内，主控自行拆分工作、选择框架和实现方法，并完成必要的审查与验证；不要求用户逐项批准普通可逆实现细节。
- 每次选择一个可观察玩家效果或明确工程问题，说明范围、关键接口和验证方式，再完成最小可复核改动。复杂任务可以并行委派，最终集成与判断仍由主控负责。
- 先检查当前状态和脏工作树，保留他人改动。不把旧实现、旧测试或工具数量当作必须继承的目标。
- Skills 是可选的专业方法，不能另设产品权威、扩大授权、锁死架构或要求已删除的控制流程。用户明确只读时严格只读。
- 本次重构不依赖旧 Project Atlas 索引。需要查询它时先核对索引所属工作目录、版本与时效，不能用旧快照覆盖当前代码和产品基线。
- 只有范围变化、关键产品分歧或独立权限边界确实需要用户决定时才询问；已有明确授权持续有效。
- 用户暂停开发时，仅执行其随后明确授权的工作。文档整理、评估和待办分层不自动恢复开发或创建目标；候选架构、工作量估算与已选定实施项明确分开。

## 源码仓库边界

- 本仓库面向游戏使用者、审阅者与贡献者，只跟踪正式运行、构建、回归测试及必要说明和许可所需文件。
- 所有实验室、候选模型与素材、历史评测报告、本机日志、临时产物和个人开发工具配置留在工作站，不并入本仓库。
- 实验中被正式采用的实现应提取为对应生产模块，同时迁移实际依赖和许可证；不得通过整体改名把实验室转成正式代码。
- 正式语音资源准备位于 `game/vendor/tts/`；模型权重、下载缓存和生成运行环境不进版本库。正式测试与合成测试夹具继续保留，不能为减少文件而破坏复现。
- AI 协作工具和模型偏好由贡献者在本机配置；仓库不指定个人模型与额度。委派需要边界清楚，最终集成与验证由主控负责。

## 保护与验证

- 保护已认可的 UI、美术、语音、内容、多语言和许可证；保护真实存档、个人数据与凭据。
- 真实或付费模型调用、真实数据读取/迁移、生产依赖安装、破坏性清理和发布操作按具体授权执行。暂存、提交、推送、PR、打包、签名与发布不由普通开发要求自动授权。
- 运行与改动匹配的检查。交互变化检查真实渲染，玩法变化验证实际流程，记忆检查跨上下文与重启后的正确使用；不以模拟数据冒充真实模型或人工验收。
- 语义错误、失败和首次异常如实记录；技术通过不等于叙事、听感、美术或发布被接受。重复测试只为解决新失败或未解疑问。

## 文档保持轻量

维护 PRODUCT.md、AGENTS.md、REBUILD.md 三个入口即可。接口细节优先放代码和必要的就近说明，测试直接对应产品行为。只有确有复用需要才增加文件，不建立重复状态页、逐轮报告流或新的归档副本。

稳定目标改变才更新 PRODUCT.md。REBUILD.md 按层维护问题、状态、依赖与完成依据；完成项合并到简短基线，替换过时的下一步，不追加逐轮流水或重复测试总数。只保留少量可回查的代表证据，缺失证据明确标注，不恢复新的历史必读页。历史实验报告留在本地工作站；不把历史“下一步”恢复为当前任务。归纳测试时保留日期、对应版本、证据层级、首次失败与未验证边界，不累加测试总数。

保留游戏运行、内容加载、打包、素材与许可证所需文件。测试记录和目录导航不代替产品决定，也不把未验证项改写成已通过。
